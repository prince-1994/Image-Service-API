version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - 8000:8000
    env_file:
      - ./.env.prod
    command: sh -c "uvicorn server.main:app --host 0.0.0.0 --port 8000 --workers 10"
    restart: always